---
import Map from './Map.astro';
---

<section id="about" class="section about">
  <div class="wrap about-grid">
    <div class="about-left">
      <p class="mono-label">Discover</p>
      <h2 class="about-title">Sulawesi<br /><em>Tengah</em></h2>
      <p class="about-body">
        We are local operators still learning every season, every road, and every crossing. From Poso highlands to
        Banggai coastlines, we build routes around real conditions and real communities.
      </p>
      <blockquote class="about-tagline">"Sulawesi has been waiting.<br />So have we."</blockquote>
      <div class="about-stats">
        <div class="stat">
          <span class="stat-num">13</span>
          <span class="stat-label">Kabupaten / Kota</span>
        </div>
        <div class="stat">
          <span class="stat-num">68K</span>
          <span class="stat-label">km² Area</span>
        </div>
        <div class="stat">
          <span class="stat-num">3M+</span>
          <span class="stat-label">Population</span>
        </div>
      </div>
    </div>

    <aside class="about-right" aria-label="Sulawesi Tengah route notes">
      <Map />
      <div class="region-cards">
        <div class="rcard">
          <div class="rcard-name">Poso</div>
          <div class="rcard-desc">Highland lakes, Tentena routes, and village-paced travel days.</div>
        </div>
        <div class="rcard">
          <div class="rcard-name">Banggai</div>
          <div class="rcard-desc">Coastal crossings, clear water, and small harbor logistics.</div>
        </div>
        <div class="rcard">
          <div class="rcard-name">Donggala</div>
          <div class="rcard-desc">West coast weather windows and long, honest road sections.</div>
        </div>
        <div class="rcard">
          <div class="rcard-name">Morowali</div>
          <div class="rcard-desc">Karst channels, river edges, and remote village support lines.</div>
        </div>
      </div>
    </aside>
  </div>
</section>

<style>
  .about {
    background: var(--cream);
    padding: 4rem 0 5.2rem;
  }

  .about-grid {
    display: grid;
    gap: 3rem;
    align-items: start;
  }

  .about-left {
    position: static;
  }

  .about .mono-label {
    color: var(--lime);
    letter-spacing: 0.2em;
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
  }

  .about .mono-label::before {
    content: '';
    display: inline-block;
    width: 1.5rem;
    height: 1.5px;
    background: var(--lime);
  }

  .about-title {
    font-size: clamp(2.35rem, 5.3vw, 3.6rem);
    line-height: 1.08;
    color: var(--forest);
    margin-bottom: 1.35rem;
  }

  .about-title em {
    font-style: italic;
    color: var(--lime-light);
  }

  .about-body {
    font-size: 0.95rem;
    line-height: 1.85;
    color: var(--mid);
    margin-bottom: 1.5rem;
    max-width: 24rem;
  }

  .about-tagline {
    margin: 0 0 1.8rem;
    padding-left: 1rem;
    border-left: 3px solid var(--tan);
    font-family: var(--font-serif);
    font-style: italic;
    font-size: 1.24rem;
    line-height: 1.55;
    color: var(--rust);
  }

  .about-stats {
    display: flex;
    flex-wrap: wrap;
    gap: 2rem;
    padding-top: 1.4rem;
    border-top: 1px solid var(--light);
  }

  .stat {
    display: grid;
    gap: 0.34rem;
  }

  .stat-num {
    font-family: var(--font-serif);
    font-size: 1.85rem;
    line-height: 1;
    color: var(--forest);
  }

  .stat-label {
    font-family: var(--font-mono);
    font-size: 0.54rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--mid);
  }

  .region-cards {
    display: grid;
    grid-template-columns: 1fr;
    gap: 0.75rem;
    margin-top: 0.9rem;
  }

  .rcard {
    background: var(--cream-2);
    border: 1px solid var(--light);
    padding: 1rem;
    transition: border-color 0.2s ease, background 0.2s ease, transform 0.2s ease;
  }

  .rcard:hover {
    border-color: var(--lime);
    background: var(--lime-pale);
    transform: translateY(-2px);
  }

  .rcard-name {
    font-family: var(--font-serif);
    font-size: 1.1rem;
    color: var(--forest);
    margin-bottom: 0.22rem;
  }

  .rcard-desc {
    font-size: 0.8rem;
    line-height: 1.55;
    color: var(--mid);
  }

  @media (min-width: 768px) {
    .about-grid {
      grid-template-columns: 1fr 1.45fr;
      gap: 4.5rem;
      align-items: start;
    }

    .about-left {
      position: sticky;
      top: 6rem;
    }

    .region-cards {
      grid-template-columns: 1fr 1fr;
    }
  }
</style>